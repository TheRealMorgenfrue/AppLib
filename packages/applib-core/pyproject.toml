[project]
name = "applib-core"
version = "0.1.0"
description = "The essential components of AppLib, like configuration and logging."
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["PyQt6==6.8.1", "tomlkit", "pydantic==2.9.2", "colorama"]
